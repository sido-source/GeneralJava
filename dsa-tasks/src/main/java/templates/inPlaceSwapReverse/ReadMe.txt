This template is used:
- we have some in-place operations
- we have pivot (the point which separates two subbarays, we need the pivot to do some manipulation on input data)
- we use 2 pointer technique to optimize solution